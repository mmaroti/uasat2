find_program(emsripten_prog em++)
if(NOT emsripten_prog)
    message("Emscripten is not found")
else()
    message("Emscripten applications are enabled")
    add_subdirectory(validate)
    add_subdirectory(testing)
endif()
